# ExtCloud

Public Cloudstream extension repository. Build artifacts are published to the `builds` branch.

Cloudstream repository URL:

```text
https://raw.githubusercontent.com/Pendztagram/extcloud/builds/repo.json
```